# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 134
--
length dropped
precision dropped
display width dropped
pg alias int4
pg alias int8
character varying
double precision
timestamptz spelled out
case + whitespace
unsigned kept
signed dropped
zerofill dropped
unsigned differs from signed
array suffix kept
array differs from scalar
charset dropped
collate dropped
int(11) key == integer key
return type not in the key
argument order matters
kind is part of the key
schema excluded by default
schema included when asked
MySQL: names fold
PG↔PG: names do NOT fold
MySQL: getValue == getvalue
PG: getValue != getvalue
reformatted twin -> Identical
identical is not a difference
identical is counted
real body change -> BodyDiffers
both sides carried for side-by-side display
source body retained verbatim
two one-sided pairs
source-only counted
target-only counted
same-engine width change -> SignatureDiffers
reason names both declarations
return type change -> SignatureDiffers
SECURITY DEFINER difference is never 'identical' -> SignatureDiffers
volatility change -> SignatureDiffers
IN -> OUT is a signature difference -> SignatureDiffers
unknown mode is not a difference -> Identical
function and procedure paired separately
both identical, no cross-pairing
identical text cross-engine is still NotComparable -> NotComparable
reason explains the refusal
sameEngine flag recorded on the set
different text cross-engine is also NotComparable -> NotComparable
cross-engine spellings matched into one pair
no phantom one-sided entries
cross-engine spelling noise is not a signature difference
unsigned vs signed is judged, one way or the other
a reason is always given
unparseable body -> NotComparable, not BodyDiffers -> NotComparable
reason mentions parsing
sql vs plpgsql -> NotComparable -> NotComparable
all four entries reported
every one is NotComparable
ambiguity reason given
ambiguity reason given
ambiguity reason given
ambiguity reason given
kind change re-joined into one pair
kind change is a signature difference -> SignatureDiffers
both sides shown
no speculative re-join with multiple leftovers
not comparable
ZERO pairs — no fabricated one-sided entries
no counted differences
status preserved for the UI
label is the one the UI shows
target denied -> zero pairs
source routines are NOT reported as source-only
unsupported -> zero pairs
unsupported label differs from permission label
two hidden bodies are NOT 'identical' -> NotComparable
reason names the privilege cause
one hidden body -> NotComparable -> NotComparable
signature difference still reported with hidden bodies -> SignatureDiffers
Ok has no label
disabled -> zero pairs
every routine on either side is accounted for
counts add up
2 identical
2 body differences
1 source-only
1 target-only
differences sort before identical
函数 category has 4
存储过程 category has 2
function differences: 3
procedure differences: 1
verdict has a label
verdict has a label
verdict has a label
verdict has a label
verdict has a label
verdict has a label
result is order-independent
source body available for display
target body available for display
a human-readable reason is present
body is not a CREATE statement
Signature() is a display string, not DDL
compare layer is independent of the execute path
baseline: full read is Ok
baseline: three routines read
baseline: signatures populated
baseline: no detail text on a clean read
the PARAMETERS query really was attempted
PARAMETERS denied => side is NOT Ok
...and is recognized as a privilege problem
...carrying the server's own error text
...which names the table that was denied
no partially-read routines are returned
a NETWORK failure on PARAMETERS degrades as Unsupported
...not as a privilege problem
...and still returns no routines
asymmetric failure yields ZERO pairs
...zero false SourceOnly
...zero false TargetOnly
...and the set reports itself not comparable
the degraded branch is the one that will render...
...and it has the error text to render
...attributed to the side that actually failed
hazard confirmed: signature-less side would double-report all 3
symmetric failure also degrades rather than guessing
a denied ROUTINES query still degrades as before
...and never asks for PARAMETERS
an empty catalog is Ok, not degraded
...with an empty list and no error text
...and the empty early-out still precedes the PARAMETERS query
the privilege probe never runs when routines were listed
