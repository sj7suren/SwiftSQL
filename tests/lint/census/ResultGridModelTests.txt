# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 71
--
click on unsorted grid -> single asc
click again on the same column -> desc
third click clears the sort entirely
click on a DIFFERENT column replaces, does not append
click collapses a multi-key sort to a single asc key
toggle appends a new key as asc
toggle flips an existing asc key in place
toggle drops an existing desc key
dropping the middle key preserves the priority of the rest
toggle on an empty list appends asc
unsorted headers are bare
single key gets an arrow and NO priority digit
unsorted columns stay bare alongside a sorted one
multi-key: first key shows priority 1
multi-key: second key shows priority 2
labels are produced one per column
empty key list is a no-op
ascending sort carries each row's orig index with its cells
descending sort likewise
numeric cells sort numerically, not lexicographically
text cells sort case-insensitively
multi-key: first key decides, ties broken by the second
equal rows preserve input order (stable sort)
out-of-range keys are skipped, later keys still apply
single row is a no-op
a new row's -1 marker survives the sort
empty block -> empty text
single cell has no separators
row is tab-separated
rows are newline-separated with NO trailing newline
empty cells are preserved as empty fields
empty clipboard is reported empty
whitespace-only newlines are reported empty
a single tab-free value is a scalar paste
scalar value round-trips
a tabbed single line is a ROW paste, not a scalar
row fields split on tab
CRLF input yields two clean rows
CR is stripped, trailing blank line dropped
multiple tab-free lines are a multi-row paste
ragged rows are preserved as-is
copy -> paste round-trips
exact multiple yields no trailing empty page
one row over the boundary adds a page
one row under stays on the same page
a single row is one page
zero rows is page 0
unknown row count pins at page 0
clamp caps at the last page
clamp floors at 0
clamp leaves a valid page alone
unknown count does not cap forward paging
no bound table shows the em-dash placeholder
no bound table disables every pager control
count pending shows the page number alone
count pending still allows paging forward
count pending disables 末页 (no known last page)
page 0 disables 首页/上一页
first of three pages
first page: back disabled, forward enabled
middle page
middle page enables all four
last page
last page: forward disabled, back enabled
empty table reads as one page
empty table disables navigation but keeps the size choice
short value passes through
newlines/tabs/CR flatten to spaces
empty stays empty
exactly 60 chars is NOT elided
61 chars is elided to 60 + ellipsis
