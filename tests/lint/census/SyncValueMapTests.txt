# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 102
--
MayEmit(Exact)
MayEmit(Coerced)
!MayEmit(Unrepresentable)
ToTypeVerdict keeps the ladders aligned
same-engine BuildBridges succeeds
same-engine yields one bridge per column
same-engine bridges are all passthrough Identity
same-engine produces no findings
same-engine zero date stays Exact
same-engine zero date value untouched
cross-engine date arms TemporalGuard
MySQL 0000-00-00 -> Unrepresentable
zero date is NOT silently coerced to NULL
MySQL 0000-00-00 00:00:00 -> Unrepresentable
a real datetime crosses Exact
tinyint(1)->boolean arms BoolToPgBool
tinyint(1)=1 -> Coerced
tinyint(1)=1 -> TRUE
tinyint(1)=0 -> Coerced
tinyint(1)=0 -> FALSE
PG boolean renders unquoted
tinyint(1)=7 -> Unrepresentable
boolean->tinyint(1) arms PgBoolToInt
PG 't' -> 1
PG 'false' -> 0
unsigned column is NOT rejected upfront
unsigned->signed arms IntRange
value that fits -> Exact
LLONG_MAX fits bigint -> Exact
2^63 overflows PG bigint -> Unrepresentable
2^64-1 overflows PG bigint -> Unrepresentable
300 fits smallint -> Exact
70000 overflows smallint -> Unrepresentable
widening int->bigint stays passthrough
blob->bytea arms BinaryReframe
binary -> Coerced
hex payload is unchanged
renders as PG bytea literal
same payload renders as MySQL hex
NULL stays NULL
NULL is Exact
empty string stays Text (not NULL)
empty string is Exact
empty string renders as ''
NULL renders as NULL
NULL through a coercing bridge stays NULL
cross-engine text arms TextValidate
clean text -> Exact
U+FFFD (failed decode) -> Unrepresentable
embedded NUL -> Unrepresentable
integer -> varchar is refused at build time
missing target column fails the build
missing target column is reported as a Finding
the surviving column still got a bridge
target NOT NULL column with no source fails the build
good cell is accepted
zero-date cell is refused
builder is blocked
refusal produced a Finding
Build() yields nullopt for a blocked row
later good cells do not un-block
RowChangeSet::Accept refuses the blocked row
blocked row is counted, not lost
no partial row entered the sample
the set is not executable
the refusal is in the findings
fresh set is executable
clean row is accepted
insert counted
row landed in the sample
row verdict is Coerced (bool rewrite)
set stays executable
INSERT renders with the coerced boolean
all rows counted
sample capped at kSampleCap
truncation is flagged
capping does not make the set unexecutable
UPDATE with no key cannot be built
DELETE with no key cannot be built
update row builds
UPDATE skips the PK in SET and escapes the quote
delete row builds
DELETE renders with a WHERE
a blocking Finding alone makes the set non-executable
a Coerced Finding does not block
PG seq fix builds without a known max
one fix for one identity column
PG fix is self-computing
PG setval statement
MySQL seq fix builds with a known max
MySQL AUTO_INCREMENT statement
MySQL fix is not self-computing
MySQL refuses when the max is unknown
...and says why
SQL Server seq fix builds
SQL Server RESEED statement
table without an identity column yields no fix
no identity column is not an error
SQLite needs no repair
SQLite skip is not an error
Oracle refuses
Oracle says why
