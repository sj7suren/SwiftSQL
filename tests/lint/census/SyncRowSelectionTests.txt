# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 70
--
compare pass ok
one update detected
the update reached the tree as a concrete row
both column leaves present
before == the TARGET's current value
after  == the SOURCE's new value
no placeholder on either side of a changed column
specifically: NOT the stale 未随比对下发 marker
the changed column is marked differing
the UNCHANGED column is NOT marked differing
...and both sides of it are the same real value
row summary counts only the differing columns
row accepted
...and it genuinely carries no prior values
row node built
column leaf present
target side states it did not reach the UI
source side is still the real value
an unknown side is NOT rendered as agreement
compare pass ok
every sampled row has a node carrying its key verbatim
update row found
the key is the canonical encoding
label and key are different strings
label is the human form
summary rows carry NO row key
compare pass ok
the set really is truncated
a truncated table carries a warning child
...saying row-level checking is unavailable, up front
...and naming the cap that caused it
the explanation comes BEFORE the sample it qualifies
TableDiff mirrors the truncation
row selection is UNAVAILABLE
a sample row exists to try to exclude
SetRowExcluded is refused on a truncated table
...and nothing was recorded
no exclusions leak into the spec
compare pass ok
two updates
one insert
one delete
not truncated, so row selection is on offer
row selection IS available here
the row to exclude was found by its key
SetRowExcluded accepted
the table is still in the spec
exactly one row excluded
one table in the data plan
the exclusion crossed the layer boundary
...as the SAME string the data layer produced
the excluded key == EncodeRowKey(rawKey), byte for byte
the other update was NOT excluded
deletes are still authorized
compare pass ok
nothing excluded to begin with
one excluded insert
one excluded update
one excluded delete
three in total
excluded inserts == detected inserts
excluded updates == detected updates
excluded deletes == detected deletes
re-including drops the update back out of the count
...and only that one
compare pass ok
nothing excluded initially
the tree-wide walk finds it
...as an update
the plan warning still has its own root
