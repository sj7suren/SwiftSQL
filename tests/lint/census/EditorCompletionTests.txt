# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 57
--
single identifier
two-part chain
three-part chain
chain stops at a non-identifier
chain stops at a paren
double dot truncates
double dot sets the flag
a clean chain is not truncated
empty input yields nothing
empty input is truncated
underscores are identifier characters
table. → its columns
table. → column category
db. → its tables
db. → table category
db name matched case-insensitively
db.table. → db-qualified columns
db.table. → column category
unknown qualifier offers nothing
truncated chain offers nothing
no schema bound → nothing offered
a real table resolves to itself
implicit alias
explicit AS alias
lower-case as alias
multi-table join binds the right one
unknown alias degrades to itself
alias matched case-insensitively
no schema callable → identity
adjacent words are read as a binding regardless of syntax
merge: order and precedence
duplicates collapse
all-empty merge is empty
typed list rendering
uniform-category list rendering
empty list renders empty
scripted hook join
scripted hook join, empty
CJK table completes to its columns
...and the pre-fix BYTE scan offered nothing
mixed CJK/ASCII identifier completes as itself
...where the pre-fix BYTE scan completed a DIFFERENT table
CJK db.table. → db-qualified columns
...pre-fix BYTE scan: nothing
CJK db. → its tables
CJK db. → table category
CJK chain stops at a non-identifier
CJK alias binds to its CJK table
malformed chain completes to nothing
...as the live popup always did
...while the pre-fix HOOK claimed a completion
leading dot completes to nothing
...while the pre-fix HOOK claimed a completion
CompleteQualified sorts
ResolveQualifier still does not
fuzz: byte scan and unified scan agree on ASCII
fuzz: the corpus actually reached the schema
