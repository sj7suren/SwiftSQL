# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 47
--
key is the stable table name
no inserts
no updates
no deletes
an empty set is not truncated
an empty set is executable
structure defaults off
no findings
a table with no changes yields an empty spec
data layer counted the inserts
inserts mapped
updates mapped
deletes mapped
structure half passed through
structure executable by default
9 rows is well under the sample cap -> not truncated
table is in the spec
structure selected by default
inserts selected by default
updates selected by default
deletes NOT selected by default
real set + checked box + switch OFF => no deletes
real set + checked box + switch ON => deletes planned
summary counts the real delete rows
set is executable before the blocking finding
data layer marked the set non-executable
non-executable state reached the selection model
its rows are still COUNTED (they must stay visible)
the finding came through for display
finding text is table.column: reason
blocked data category not pre-checked
table present only for its still-executable structure half
structure half still plannable
blocked inserts excluded despite being force-checked
blocked updates excluded
blocked deletes excluded
no blocked rows are counted as planned inserts
a fully blocked table cannot appear in the spec at all
data layer says small set is not truncated
data layer says over-cap set IS truncated
all rows still counted, not just the sampled ones
row selection offered for the fully materialized diff
row exclusion accepted there
row selection NOT offered for the truncated diff
row exclusion refused there
truncated table still plans its whole insert category
and carries no row-level decisions
