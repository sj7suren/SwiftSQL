# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 31
--
compile-time guarantees hold
unqualified reports no schema
MySQL key == the bare name it replaced
MySQL display == the bare name
name half
schema half is empty, not invented
PG public key
PG archive key
two same-named PG tables are NOT equal
their keys differ
same name half is not sufficient for identity
qualified != unqualified
ordering is schema-major
round-trip bare
round-trip qualified
round-trip other schema
round-trip empty
round-trip dot in the table name
round-trip dot in both halves
round-trip backslash in the name
round-trip backslash and dot
round-trip trailing backslash
round-trip unicode
dotted bare name and a.b pair have DIFFERENT keys
dotted bare name round-trips as unqualified
the a.b pair round-trips as qualified
a.b pair parses back to schema a
equal names produce equal keys
wchar_t* and wxString constructions agree
IsEmpty tracks the NAME half
schema alone is not an identity
