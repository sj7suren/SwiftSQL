# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 73
--
forward order verdict
reversed order verdict
reordering the compare changes NOTHING about what executes
...and the render is non-empty
orders carries its own identity in the data plan
items too
a gesture on an unknown table is loud
...and nothing was selected as a side effect
the compare gained no phantom table
ticking 删除 while disarmed is recorded AND reported inactive
the check bit really was set (this is not a no-op)
...and IsActive still says no
NO delete is reachable anywhere in the plan
a delete-only table drops out of the plan entirely
the destructive-confirmation count is zero
but the run is not empty — inserts still go
with the switch on, the tick is simply applied
orders may delete
audit — unticked — may not, and is absent
exactly one table carries a delete authorization
one delete row is what the confirmation will be about
turning the switch back off re-strips every DELETE
...and the confirmation count follows it back to zero
data-only run is its own verdict, not an error
orders present
inserts allowed
updates NOT allowed
deletes NOT allowed
the render says so verbatim
a table whose ONLY change is a delete drops out
updates-only: updates allowed
updates-only: inserts NOT allowed
items, an insert-only table, is absent from an updates-only run
the fixture produced an insert row with a key
the exclusion was applied
orders present
exactly one row excluded
...and it is the PRODUCER's key, byte for byte
a key no row carries is refused, loudly
...and nothing was stored
one delete row excluded
the armed delete is now excluded, so nothing will be deleted
the table still carries the delete AUTHORIZATION, though
the fixture really is blocked now
ticking a blocked category is reported inactive, not applied
...and so is its delete, which has TWO reasons to be refused
the blocked table has NO data spec at all
its neighbour is unaffected
the blocked table's DDL still reaches the plan
but its data still does not
one preamble statement
one postamble statement
FK checks are relaxed before the run
...and restored after it
both are visible in the scripted render
a one-table selection still carries the preamble
...and the postamble
structure-only has its own OK token, not an error shape
the data plan is empty
...which is NOT the same as nothing to do
the DDL is there
ticking 插入 on a structure-only compare is inactive
a cross-engine pair cannot select data
the DataExecPlan is empty even with every data box ticked
the structure half still runs
an empty selection says so
nothing at all would run
no adopted plan is distinguishable from an empty selection
the typo is reported per gesture
...and it outranks the run's own verdict
an unknown category is named, not swallowed
the report leads with the verdict
and echoes the offending directive
