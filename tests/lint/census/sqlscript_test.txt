# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 80
--
empty
whitespace-only
line-comment-only
block-comment-only
single, no terminator
two statements
trailing statement without terminator
semicolon inside string literal
doubled-quote escape ('')
semicolon inside double-quoted identifier
semicolon inside backtick identifier (mysql)
mysql backslash-escaped quote
trailing line comment (--)
trailing hash comment (# , mysql)
executable comment /*! ... */ (mysql) survives
block comment bundles into following statement
DELIMITER // keeps inner ';' , splits on //
dollar-quote body keeps inner ';' and layout intact
named dollar tag $tag$ ... $tag$
bare $1 param is not a dollar-quote
mysql: \' escapes, stays one statement
postgres: no backslash escape, splits into two
empty statements between ';' are dropped
only semicolons yield nothing
unclosed string to EOF returns partial (no hang/crash)
trailing comment does not create a 2nd statement
CREATE TABLE touches tables
lower-case verb is recognised
CREATE TABLE IF NOT EXISTS
CREATE TEMPORARY TABLE
DROP TABLE
DROP TABLE IF EXISTS
ALTER TABLE
RENAME TABLE (MySQL)
TRUNCATE with the optional TABLE keyword
TRUNCATE without it — still only ever a table
CREATE VIEW is NOT a table
CREATE OR REPLACE VIEW is NOT a table
CREATE INDEX is NOT a table
CREATE DATABASE is NOT a table
CREATE FUNCTION is NOT a table
INSERT is not DDL
SELECT is not DDL
UPDATE is not DDL
empty statement
a leading -- comment banner does not hide the verb
a leading /* */ comment does not hide the verb
a MySQL # comment does not hide the verb
a script with one CREATE TABLE among DML needs a reload
a DML-only script must NOT trigger a catalog reload
SELECT is explainable
WITH is explainable
UPDATE is explainable
DELETE is explainable
a comment banner does not hide the verb
CREATE TABLE is NOT explainable
SET is NOT explainable
SHOW is NOT explainable
empty is NOT explainable
comment-only is NOT explainable
MySQL: plain EXPLAIN
MySQL: plain EXPLAIN (no ANALYZE)
Postgres: VERBOSE+COSTS, planner only
Postgres: VERBOSE+COSTS, planner only (no ANALYZE)
SQLite: QUERY PLAN (not the opcode dump)
SQLite: QUERY PLAN (not the opcode dump) (no ANALYZE)
Oracle: two steps, plan read back by the second
Oracle: two steps, plan read back by the second (no ANALYZE)
SQL Server: no usable non-executing plan -> nothing
SQL Server: no usable non-executing plan -> nothing (no ANALYZE)
DELETE on PG is planner-only
DELETE on PG is planner-only (no ANALYZE)
UPDATE on MySQL is planner-only
UPDATE on MySQL is planner-only (no ANALYZE)
a trailing ';' is stripped
a trailing ';' is stripped (no ANALYZE)
surrounding whitespace is trimmed
surrounding whitespace is trimmed (no ANALYZE)
un-explainable statements produce nothing, on every dialect
un-explainable statements produce nothing, on every dialect (no ANALYZE)
