# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 58
--
MySQL: a doubled backtick un-doubles to one literal backtick
...and is NOT the quote-stripped `weird`
PG: a doubled double-quote un-doubles to one literal quote
PG: a backtick inside a quoted name is data, not a delimiter
MySQL: a double quote inside a backtick name is data
MySQL: a dot inside a quoted name is not a qualifier
PG: a dot inside a quoted name is not a qualifier
an unquoted CJK name is one identifier
a quoted name with a space survives
bare table
lower-case keywords
trailing semicolon
an alias does not change the base table
WHERE / ORDER BY / LIMIT are fine
newlines and tabs between clauses
case is PRESERVED — it may matter to the server
a table named from_log is a name, not the FROM keyword
a line comment naming another table is ignored
a block comment between tokens is ignored
MySQL # line comment
a string literal containing ' from ' is not a FROM clause
MySQL: `x--1` is arithmetic, not a comment
PG: bare -- starts a comment
PG: $$ … $$ contents are a literal, not a FROM clause
PG: a tagged $tag$ … $tag$ literal likewise
PG: $1 is a parameter, not a dollar-quote opener
MySQL: db.table splits into qualifier + table
MySQL: a QUOTED qualifier un-doubles too
PG: schema.table splits
a three-part name is refused
no qualifier written → bind
MySQL: qualifier == current db → same table, bind
MySQL: a DIFFERENT db is refused, not silently dropped
MySQL: db names are case-sensitive — no case-insensitive match
MySQL: no current db selected → a qualifier cannot be verified
PG: a schema qualifier is always refused
a join
a union
group by
distinct
an aggregate
a comma join
a derived table
a scalar subquery
no FROM at all
not a SELECT
empty input
a CTE is not a simple select
an unterminated quoted identifier is Unparsable
an unterminated string literal is Unparsable
an unterminated block comment is Unparsable
SQL Server [brackets] are an unmodelled quoting form
Unparsable does not bind
UnknownQuoting does not bind
NotSimple does not bind
MySQL: parsed name re-quotes to the ORIGINAL written form
PG: parsed name re-quotes to the ORIGINAL written form
a dotted name re-quotes as ONE identifier, not a qualified pair
