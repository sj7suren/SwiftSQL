# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 18
--
unconfigured selection still wins
unconfigured selection is reported as unusable
configured selection wins
configured selection is usable
no selection → first provider carrying credentials
nothing configured → first provider
stale selection falls back to a real row
no providers at all → nullptr
ollama is usable without a key
openai-compat is not usable without a key
anthropic is not usable without a key
identical configs share a key
kind change invalidates the key
baseUrl change invalidates the key
apiKey change invalidates the key
model change invalidates the key
name/id are not part of the wire identity
key fields cannot smear into each other
