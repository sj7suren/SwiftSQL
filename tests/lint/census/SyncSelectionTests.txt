# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 120
--
equal names compare equal
different names compare unequal
ordering is over the identity string
default-constructed key is empty
Value() round-trips
all four tables adopted
orders structure checked by default
orders inserts checked by default
orders updates checked by default
orders DELETES off by default (product safety rule)
master delete switch off by default
products has no inserts -> not checked
products structure checked
invoices inserts blocked -> not checked
invoices updates blocked -> not checked
Keys() returns every table
Keys()[0] sorted by identity
Keys()[3] sorted by identity
SetChecked on unknown table returns false
unknown table stays unknown
table count unchanged after unknown write
check orders deletes
check-bit is set
but NOT active while master switch is off
orders is in the spec (its other categories are selected)
spec MUST NOT contain deletes with master switch off
non-delete categories unaffected
ContainsDeletes() false across the whole spec
summary delete count is 0 with master switch off
ALL delete boxes checked + switch off => still zero deletes
summary still reports 0 deletes
master switch alone does NOT arm deletes
orders deletes not active with box unchecked
both gates open => active
both gates open => deletes in spec
ContainsDeletes() true
summary counts the 3 deletes
closing the master switch retracts deletes from the spec
the per-table check bit is REMEMBERED across the toggle
summary delete count back to 0
SetAllTablesChecked does not touch the delete category
...so even with the master switch on, no deletes are planned
invoices inserts not active (blocking findings)
invoices updates not active
invoices deletes not active
a fully-blocked table is absent from the spec entirely
blocked structure is not pre-checked
mixed table present for its executable data half
blocked structure excluded even when force-checked
executable inserts still included
products present for its structure change
an empty category is not emitted even when checked
clearing every box yields an empty spec
empty spec has no tables
row selection available for a non-truncated diff
excluding a row succeeds
excluding a second row succeeds
the row reads back as excluded
two rows excluded
spec carries the row exclusions
spec exclusion count
summary reports excluded rows
re-including a row succeeds
row no longer excluded
ClearRowExclusions empties the set
row selection NOT available for a truncated diff
SetRowExcluded refused on a truncated diff
nothing was recorded for the truncated table
truncated table still plans its whole categories
truncated table's inserts included
truncated table carries no row exclusions
row selection unavailable for unknown table
SetRowExcluded on unknown table returns false
an empty row identity is refused
orders structure cleared
products structure cleared
customers (not visible) untouched
orders structure restored
invert cleared orders structure
invert set products structure
invert did not touch the inserts category
invert set the orders delete check-bit
but deletes remain inactive (master switch still off)
and remain absent from the spec
no phantom table was created
sweep off clears structure
sweep off clears inserts
sweep off clears updates
SetTableChecked restores structure
SetTableChecked leaves deletes off
baseline spec is exactly the asymmetric selection made above
REGRESSION: reversing the compare result changes nothing
REGRESSION: rotating the compare result changes nothing
all 24 permutations of 4 tables were exercised
REGRESSION: the spec is identical under EVERY permutation
Find() still resolves orders after reordering
...and returns ORDERS' own stats, not a neighbour's
table count follows the new result
vanished table pruned
surviving table keeps its user selection
surviving table keeps its cleared boxes too
surviving table keeps its row exclusions
newly appeared table gets seeded defaults
newly appeared table's deletes still default off
a now-blocked category is unchecked on rebind
orders drops out of the spec entirely
row excluded while small
row exclusions dropped once the diff became truncated
row selection no longer offered
an identity-less table is refused
3 tables selected by default
2 tables carry structure
inserts summed across selected tables
updates summed across selected tables
deletes are 0 (default off + master switch off)
deletes summed once both gates are open
deletes drop back to 0 the instant the master switch closes
empty model summarizes to zero tables
empty model summarizes to zero inserts
empty model builds an empty spec
