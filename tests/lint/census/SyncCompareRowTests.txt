# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 71
--
Create  -> 新建表
Drop    -> 目标多余表
Structure -> 仅结构
Data    -> 仅数据
Both    -> 结构+数据
every type has a non-empty label
clean diff -> 就绪
就绪 is not a warning
truncated -> 行数超限
blocked outranks truncated
a blocked STRUCTURE half also reports 含不可转换值
every non-ready status is a warning
NoPrimaryKey   -> 无主键（已排除）
UnorderableKey gets its OWN status, not a generic error
TargetMissing  -> 目标缺少此表
Blocked        -> 含不可转换值
NotRequested is not an error state
the verdict enum maps one-for-one
every refusal has a user-facing label
and the labels are distinct from one another
one TableDiff per unit
key is the stable table name
ddl present -> hasStructure
inserts mirrored from the RowChangeSet
updates mirrored from the RowChangeSet
deletes mirrored from the RowChangeSet
a small diff is not truncated
an unblocked set is executable
verdict carried through
plan-global warnings are NOT attributed to a table
data-only table has no structure half
a blocked set arrives as NOT executable
and reads as 含不可转换值
the per-table verdict reason IS attributed to its table
a capped diff arrives as truncated
and reads as 行数超限
row-level selection is refused on the truncated table
a blocked table has no executable data category
both tables are authorized to write
lookup is by table name
inserts authorized
updates authorized
deletes NOT authorized under the default selection
both tables survive
plan-global preamble passes through
plan-global postamble passes through
warnings pass through
structure checked -> ddl kept
the preview dml vector is not propagated into the execution plan
the unit survives for its data half
but carries no ddl
the raw check bit did flip
but the category is not ACTIVE
spec contains no deletes while the switch is off
no table is authorized to delete
no table is authorized to delete
with the master switch on, orders may delete
a table whose delete box was never ticked still may not
events had only inserts, so unchecking them removes it entirely
orders is untouched
the spec itself is empty
no structure units
no data authorization
the table accepts row-level selection
the row is excluded
the exclusion reached the db-layer spec
and its string is untouched
same unit count after reordering the plan
the surviving table is 'orders' either way
one authorized table
and it is orders
