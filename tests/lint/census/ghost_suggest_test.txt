# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 38
--
SELECT then space suggests ' * from '
upper-case SELECT too
mixed case
after a newline and indentation
INSERT -> into
DELETE -> from
ORDER -> by
GROUP -> by
LEFT -> join
CREATE -> table
DROP -> table
IS -> null
no space yet -> the popup owns it, stay quiet
half-typed word -> quiet
text after the caret -> quiet
even a single token after -> quiet
trailing whitespace after the caret is still end-of-line
inside a single-quoted literal -> quiet
inside a double-quoted literal -> quiet
inside a backquoted identifier -> quiet
after a -- line comment -> quiet
after a # line comment -> quiet
inside a /* block comment -> quiet
a CLOSED literal does not silence the rest of the line
a CLOSED block comment does not silence it
a line comment ENDED by a newline does not silence it
'' escaped quote inside a literal keeps it closed
ORDER BY does not then suggest for BY
GROUP BY likewise
INSERT INTO -> the table name is the user's
DELETE FROM likewise
LEFT JOIN -> the table name is the user's
CREATE TABLE -> the name is the user's
empty buffer
whitespace only
an ordinary identifier is not a keyword
ORDERS is not ORDER
punctuation tail
