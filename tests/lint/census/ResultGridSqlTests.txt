# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 110
--
identifiers quote through db::QuoteIdent (MySQL)
identifiers quote through db::QuoteIdent (PG)
RenderValue NULL marker
RenderValue plain
RenderValue empty
RenderValue apostrophe
RenderValue injection
PINNED (not fixed): cannot store the literal string NULL
QualifyForDml no db
QualifyForDml MySQL db-qualified
QualifyForDml PG ignores db
MapPkIndices maps in pkColumns order, not column order
BuildPkWhere composite
MapPkIndices reports a partial key as a short vector
MissingPkColumns names exactly the absent key column
MissingPkColumns is empty when the whole key is present
MissingPkColumns reports every absent column, in declaration order
BuildPkWhere refuses a partial key
clean grid emits nothing
one edited cell -> one-column UPDATE
two edited cells in one row -> one UPDATE, two SETs
edited PK keys the UPDATE off the ORIGINAL value
new row -> INSERT of non-empty cells only
entirely empty new row emits nothing
DELETEs are emitted before INSERTs
value -> NULL becomes SET col=NULL
NULL is not confused with empty string
no PK in the result -> no statements at all
...but the edit is still reported as pending, not as clean
...and the gate says the key is incomplete
table with no PK at all -> no statements
table with no PK at all -> NoPrimaryKey, not IncompleteKey
Postgres quoting
browse-mode DML is db-qualified
FIXED: partial composite PK emits NO under-constrained UPDATE
FIXED: the refusal is reported, not silent — gate + missing column
FIXED: the pending edit is still visible to the caller
PK column present but EMPTY still passes the gate (presence != value)
fully-present composite PK still emits both key columns
single-column PK edit is completely unaffected by the new gate
single-column PK edit passes the gate cleanly
a clean grid is Ok-but-not-dirty (distinct from a refusal)
clean grid is not dirty
an edited cell is dirty
a deleted row is dirty
a new row with content is dirty
an entirely empty new row is NOT dirty
dirty is independent of the PK gate
INSERT statements name every column
INSERT falls back to the table_name placeholder
no columns -> no INSERTs
UPDATE statements exclude the PK from SET
DELETE statements key on the PK
partial composite PK refuses to generate UPDATEs
partial composite PK refuses to generate DELETEs
no PK at all refuses to generate row DML
composite PK WHERE carries every key column
composite PK UPDATE sets only the non-key column
clipboard DML escapes apostrophes
QualifyForPaging MySQL db-qualified
QualifyForPaging MySQL no db
QualifyForPaging PG ignores db
no keys -> empty ORDER BY body
single key
multi-key keeps priority order
out-of-range keys are skipped
page SELECT, first page, no filter/sort
page SELECT offsets by page*size
page SELECT carries WHERE and ORDER BY in that order
Oracle page SELECT uses OFFSET/FETCH
filter by value
exclude value
filter by NULL uses IS NULL
exclude NULL uses IS NOT NULL
filter escapes apostrophes
filter PG quoting
db::QuoteIdent doubles an embedded backtick
db::QuoteIdent doubles an embedded double quote (PG)
FIXED: DML path escapes the table name
pager path escapes the same table name identically
FIXED: the two paths now agree (the defect is gone)
both halves of db.table are escaped independently
FIXED: UPDATE escapes both the SET column and the WHERE key
FIXED: INSERT escapes its column list
FIXED: DELETE escapes the WHERE key
FIXED: 复制为 INSERT escapes table and columns
FIXED: 复制为 UPDATE escapes table, SET column and WHERE key
FIXED: 复制为 DELETE escapes table and WHERE key
FIXED: cell filter escapes the column name
FIXED: cell filter escapes the column name on IS NULL too
FIXED: PG DML escapes the table name
FIXED: PG UPDATE escapes the SET column and the WHERE key
FIXED: PG cell filter escapes the column name
a double quote is not special to MySQL and is not doubled
a backtick is not special to PG and is not doubled
an ordinary name is wrapped exactly once
Ok reports OK
an already-equal cell is UNCHANGED, not an error
an empty result is NO_ROWS, not an error
a refused bind is REFUSED:NOT_EDITABLE and carries the reason
a refused bind with no stated reason still reports the refusal
no primary key at all
an incomplete composite key names the missing columns
every outcome has its own token
no non-Ok outcome starts with OK
no non-Ok outcome starts with OK
no non-Ok outcome starts with OK
no non-Ok outcome starts with OK
no non-Ok outcome starts with OK
no non-Ok outcome starts with OK
