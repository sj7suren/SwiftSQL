# assertion census — generated by tests/lint/assertion_census.py --update
# DO NOT HAND-EDIT. See the header of that script for what this protects.
state: RAN
checks: 45
--
virgin scope has no groups
virgin scope has no memberships
AddGroup accepts a new name
AddGroup accepts a second name
AddGroup refuses a duplicate
AddGroup refuses an empty name
AddGroup refuses a whitespace-only name
groups survive the ini round-trip IN CREATION ORDER
an empty group still persists (nothing has been assigned yet)
member reads back its group
three memberships stored
assigning to a NON-EXISTENT group is refused, not auto-created
...and no phantom group appeared in the list
a member never assigned is ungrouped
a member moves between groups
an empty group name moves the member back to top level
...and it is no longer counted as a membership
RenameGroup accepts a fresh name
the rename is in the list, in place
member 1 came WITH the rename
member 2 came WITH the rename
RenameGroup refuses a name that already exists
RenameGroup refuses an unknown source group
MembersOf returns only that group's members, sorted
MembersOf of a non-existent group is empty
ClearGroup emptied it
...but the GROUP itself survives, unlike RemoveGroup
...and its members are ungrouped, not deleted — still assignable
...proved by refilling the emptied group
ClearGroup left the other group's member alone
the deleted group is gone from the list
its member is UNGROUPED, not deleted — it is still assignable
...proved by re-assigning that same member to the surviving group
a renamed member keeps its group
...and the old name no longer resolves
a removed member's assignment is forgotten
object scope: AddGroup
the object scope has its OWN group list
the connection scope is untouched by it
a member name that exists in both scopes resolves per scope
...and is ungrouped in the other scope
a different DATABASE is a different scope
a different CATEGORY is a different scope
groups survive a cold reopen of the file
memberships survive a cold reopen of the file
